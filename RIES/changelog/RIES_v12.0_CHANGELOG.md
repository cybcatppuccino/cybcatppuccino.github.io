# RIES v12.0 changelog

- Bumped the visible RIES version and browser cache-busters to v12.0.
- Consolidated the historical per-version JS regression files into five module-oriented v12.0 suites plus one shared test utility and one all-suite runner.
- Added coverage checks for exported module hooks, lazy database loading, hypergeom Re/Im projection search, transformed exp/log database rows, LaTeX escaping, typed precision, integer validation, L-function sorting/runtime rows, and confidence sorting.
- Kept the v11.9.2 hypergeom assets unchanged and cache-busted them through v12.0 URLs.
