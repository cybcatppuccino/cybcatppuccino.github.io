# RIES v10.4

- Adjusted confidence sorting to use module-wise round-robin ordering after sorting each module by visible formula length/compactness.
- Irreducible algebraic rows, including exact-input algebraic rows, now obey the same length-first ordering as L-function, logarithmic, RIES and constant rows.
- Short L-function and logarithmic explanations are no longer pushed behind longer algebraic relations solely because the algebraic residual is exact or higher precision.
- Kept v10.3 integer-mode exact display validation and packed 100k database.
